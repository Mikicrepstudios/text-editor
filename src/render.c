#include <ncurses.h>


void Render() {
    // Get max number of rows and columns
    int terminalWidth = getmaxx(stdscr);
    int terminalHeight = getmaxy(stdscr);

    // TODO: Do actual rendering code using linked lists
}